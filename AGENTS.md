# EdSync Agent Instructions

- Work directly on `main` only unless the user explicitly says otherwise.
- Do not create feature branches or worktrees for this repository.
- Commit every edited, added, or removed file separately.
- Push `main` to GitHub after every successful commit.
- Keep secrets out of tracked files. Use process environment variables or provider secret stores only.
- Preserve EdSync-specific Cloudflare resources separately from AllChess and LEARN.
