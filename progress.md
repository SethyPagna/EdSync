# EdSync Improvement Progress

**Last Updated:** 2026-05-21

**Current Track:** Comprehensive workflow, design, architecture, AI, templates, lessons, slides, discussions, quizzes, activities, and tracking improvements.

**Repository Rules:**
- Branch: `main`
- Commit pattern: one edited/added/removed file per commit
- Push pattern: push `main` after every successful commit
- Secrets: never commit secrets
- Cloudflare resources: keep EdSync separate from AllChess and LEARN

---

## Status Summary

| Phase | Name | Status | Target Outcome |
| --- | --- | --- | --- |
| 1 | Product Inventory And Baseline | Complete | Existing features, routes, and data models are mapped in `docs/BASELINE_INVENTORY.md`. |
| 2 | Workflow Map And Navigation Model | Complete | Teacher workflow, navigation handoffs, and saved states are mapped in `docs/WORKFLOW_NAVIGATION_MODEL.md`. |
| 3 | Core Learning Object Architecture | In progress | Shared content model foundation exists; page and API adoption is next. |
| 4 | Studio Component Refactor | Not started | Studio is split into maintainable modules. |
| 5 | Template System Foundation | Not started | Reusable generated/manual templates are modeled and searchable. |
| 6 | Template Application And Auto Update | Not started | Template changes update full lesson packages while preserving content. |
| 7 | Template Import, Export, And Marketplace Readiness | Not started | Templates can be safely imported/exported. |
| 8 | AI Prompt Contract System | Not started | Every AI tool uses targeted prompts and schemas. |
| 9 | AI Response Validation, Repair, And Selective Import | Not started | AI responses are validated, repaired, previewed, and selectively inserted. |
| 10 | Lesson Builder Upgrade | Not started | Manual and generated lessons share a rich editable structure. |
| 11 | Slide And PPT Design System | Not started | Slides and PPT exports are polished and template-aware. |
| 12 | Discussion Engine | Not started | Lessons generate structured classroom and online discussions. |
| 13 | Quiz And Assessment Engine | Not started | Quizzes support diverse question types and analytics. |
| 14 | Activity And Practice Diversity | Not started | Lessons generate varied activities and practice modes. |
| 15 | Media, Import, And Source Handling | Not started | Diverse source imports generate traceable learning packages. |
| 16 | Collaboration, Versioning, And Review | Not started | Content has snapshots, review states, and restore paths. |
| 17 | Personalization And Accessibility | Not started | Content adapts to learners and passes accessibility checks. |
| 18 | Analytics And Continuous Improvement | Not started | Usage and assessment data improve learning content. |
| 19 | Quality Gates, Testing, And Deployment Workflow | Not started | Tests and deployment checks protect the platform. |
| 20 | Launch Readiness And Operating Rhythm | Not started | Delivery rhythm, docs, and smoke tests are stable. |
| 21 | Public Intro, Workflow Slides, Search, And Launch Motion Redesign | Planned | Intro/workflow/search polish is tracked as the next public launch refinement. |
| 22 | Deep Data Architecture, Schema, And Learning Flow Audit | Complete | Actual D1 schema, code access paths, relationships, risks, and improvement proposal are documented. |

---

## Current Focus

**Next Target:** Phase 3, Core Learning Object Architecture.

**Immediate Tasks:**
- [x] Inventory routes by role and feature.
- [x] Inventory data models and migrations.
- [x] Inventory AI endpoints, prompt contracts, and validators.
- [x] Inventory Studio, lesson, slide, practice, quiz, discussion, and template capabilities.
- [x] Mark capabilities as `keep`, `refactor`, `merge`, or `replace`.
- [x] Define the teacher idea-to-assignment workflow.
- [x] Define canonical handoffs between Studio, Lessons, Slides, Practice, Discussions, and Analytics.
- [x] Define saved-state labels and where they appear in navigation.
- [x] Define shared `LearningObject`, `LearningBlock`, and workflow-state types.
- [x] Add legacy adapters for Studio documents, lesson sections, quiz questions, and content blocks.
- [x] Add tests for normalization from existing records into the shared model.
- [x] Export shared learning-object helpers into the first teacher lesson or Studio integration point.
- [x] Replace duplicated content-block status/type display in Studio with shared adapters.
- [ ] Replace duplicated section and quiz normalization in large lesson page files with shared adapters.
- [x] Add teacher lesson package summary integration using `lessonRowsToLearningObject`.
- [x] Audit D1 schema, serializers, service code, API routes, and legacy client data access.
- [x] Document detailed relationships, JSON/document fields, current flow strengths, weak spots, and schema improvement order.

**Known Starting Context:**
- Studio already contains lessons, notes, docs, sheets, slides, practice, content blocks, local drafts, server save/publish/archive/delete, simple templates, transitions, animations, and AI entry points.
- A related detailed plan already exists at `docs/superpowers/plans/2026-05-16-edsync-lessons-slides-platform.md`.
- Phase 1 inventory is recorded at `docs/BASELINE_INVENTORY.md`.
- Phase 2 workflow and navigation model is recorded at `docs/WORKFLOW_NAVIGATION_MODEL.md`.

---

## Decision Log

| Date | Decision | Reason |
| --- | --- | --- |
| 2026-05-16 | Use `plan.md` for the comprehensive roadmap and `progress.md` for execution tracking. | The user requested simple root-level tracking files for better continuity. |
| 2026-05-16 | Treat generated and manual lessons as the same editable learning package model. | This avoids separate workflows that drift apart and makes templates reusable. |
| 2026-05-16 | Protect teacher-authored content when templates are reapplied. | Template changes should improve design without destroying classroom edits. |
| 2026-05-16 | Require schema validation and selective import for AI output. | AI content should be useful drafts, not unchecked writes into lessons. |
| 2026-05-16 | Treat `/studio` as the canonical authoring workspace and keep `/notes`, `/docs`, `/sheets`, `/slides`, `/practice`, `/quizzes`, and `/games` as stable entry routes. | The inventory shows these routes already exist and can converge around one workflow without breaking links. |
| 2026-05-16 | Start architecture work by extracting large teacher lesson, student lesson, and Studio files before adding more heavy UI. | The largest files are 2355, 2111, 1782, and 1394 lines and concentrate too much logic. |
| 2026-05-16 | Use one product spine from catalog or organization portal through Studio, assignment, student practice, grade events, feedback, and content improvement. | The app has many capable pages; the main maturity gap is making them behave like one learning loop. |
| 2026-05-16 | Standardize saved states as local draft, server draft, needs review, published, assigned, archived, and conflict. | Users need consistent status language across Studio, lessons, practice, assignments, and catalog publishing. |
| 2026-05-16 | Keep the first learning-object layer as adapters over current tables instead of a schema-breaking rewrite. | Existing routes and D1 data stay stable while Studio, lessons, quizzes, and content blocks converge through shared contracts. |
| 2026-05-16 | Compose legacy lesson rows into learning packages before changing database schema. | Teacher/student lesson pages can adopt the shared model incrementally while current D1 rows remain compatible. |
| 2026-05-18 | Treat `docs/DATA_SCHEMA_AUDIT.md` as the current source for schema and data-flow improvement priorities. | The audit verifies migrations against serializers and API/page access paths, then orders the next architecture work around tenant context, feature APIs, Studio as source, and event-derived grades. |

---

## Verification Log

| Date | Scope | Command | Result |
| --- | --- | --- | --- |
| 2026-05-16 | Planning docs | Manual Markdown review | Passed |
| 2026-05-16 | Phase 1 inventory | Route, migration, schema, API, Studio, and large-file scan | Passed |
| 2026-05-16 | Phase 2 workflow model | Workflow, navigation, handoff, saved-state, and next-action review | Passed |
| 2026-05-16 | Phase 3 learning-object foundation | `npm.cmd run typecheck`; `npm.cmd run test -- src/lib/learning/objects.test.ts` | Passed |
| 2026-05-16 | Phase 3 lesson-package composer and Studio integration | `npm.cmd run typecheck`; `npm.cmd run test -- src/lib/learning/objects.test.ts src/lib/learning/lesson-package.test.ts`; `npm.cmd run lint` | Passed |
| 2026-05-16 | Phase 3 teacher lesson package summary | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test -- src/lib/learning/lesson-package.test.ts` | Passed |
| 2026-05-16 | Public intro launch redesign | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; headless Chrome and Playwright screenshots at desktop/mobile/gallery viewports | Passed |
| 2026-05-16 | Interactive workflow showcase redesign | `npm.cmd run typecheck`; `npm.cmd run lint`; Playwright screenshots at hero, workflow desktop, and workflow mobile viewports | Passed |
| 2026-05-16 | Public intro performance pass | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; desktop/mobile workflow smoke review | Passed |
| 2026-05-17 | Intro-to-workflow gallery refinement | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; browser screenshots at hero, transition, workflow desktop, and workflow mobile viewports | Passed |
| 2026-05-17 | Concrete EdSync slide-scenes redesign | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; browser screenshots at hero, workflow, search, results, and mobile viewports | Passed |
| 2026-05-17 | Dark reference-style catalog hero | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; browser screenshot review for hero and workflow contrast | Passed |
| 2026-05-17 | LEARN-style intro link and workflow gallery pass | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; browser screenshots for `/`, `/showcase`, and mobile intro links | Passed |
| 2026-05-17 | LEARN-style public launch redo | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; in-app browser hero/workflow/theme/language QA; Chrome desktop/mobile screenshots | Passed locally |
| 2026-05-18 | Phase 22 schema and learning-flow audit | Migration, serializer, API route, tenant helper, event helper, generic data access, and `edsync.from(...)` sweep | Passed |
| 2026-05-18 | Phase 22 hot-path index migration | Manual SQL review for additive `CREATE INDEX IF NOT EXISTS` migration on lesson, assignment, progress, submission, catalog, tenant link, and notification paths | Passed |
| 2026-05-18 | Cloudflare D1 remote migration | `npm.cmd run db:migrate` against `edsync-dev-d1`; final index migration processed 10 queries and wrote 10 index changes | Passed |
| 2026-05-18 | Worker-native D1 adapter | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; `npm.cmd run deploy:cloudflare:app`; live `/catalog` and `/api/catalog` smoke | Passed |
| 2026-05-18 | Active tenant context | `npm.cmd run typecheck`; `npm.cmd run lint`; `npm.cmd run test`; `npm.cmd run build`; `npm.cmd run deploy:cloudflare:app`; live `/catalog` and `/api/catalog` smoke | Passed |

---

## Blockers

- None for Phase 2.

## Risks To Watch

- Studio is already large; implementation should refactor before adding heavy new UI.
- AI generation can truncate or return malformed JSON; validators and repair paths must come before broader generation features.
- Template auto-update can overwrite teacher intent unless protected fields and previews are implemented early.
- PPT export can diverge from web preview unless slide layout tokens are shared.
- Analytics must remain tenant-safe and role-safe.
- Public language copy currently includes mojibake in several non-English entries and should be corrected during the i18n polish pass.
- Generic data access and tenant read scoping remain architecture risks for later phases.

## Phase 1 Baseline Summary

- Route inventory is grouped by public/auth, admin, teacher, student, Studio/tooling, and API groups in `docs/BASELINE_INVENTORY.md`.
- Data model inventory is grouped by identity/tenancy, lessons/progress, work/grades/discussions, Studio/practice, media/import/standards, AI/automation/security, and billing/certifications/offline/analytics.
- Capability decisions are marked as `keep`, `refactor`, or `merge`.
- Major overlap areas are lesson sections vs Studio documents/content blocks, slide summaries vs lesson slides, practice items vs quiz/work questions, prompt contracts vs workflow prompts, and public org selection vs auth organization mode.
- The first refactor candidates are `src/app/teacher/lessons/[id]/page.tsx`, `src/app/student/lessons/[id]/page.tsx`, `src/app/teacher/lessons/create/page.tsx`, and `src/components/studio/StudioWorkspace.tsx`.

## Phase 2 Workflow Summary

- The canonical product spine is visitor discovery, auth/organization context, role dashboard, Studio authoring, lesson package creation, assignment, student learning, grade events, feedback, recommendations, and content improvement.
- Teacher creation flows now have a defined path from topic/file/template/previous lesson to Studio source, lesson package, slides, practice, discussion, publish, assign, review, and improve.
- Student flows are grouped into home, learn, practice, discuss, reflect, and feedback.
- Handoff payloads require shared metadata such as tenant, owner, source, title, audience, objectives, tags, language, duration, difficulty, status, and version.
- Saved states are standardized as `Local draft`, `Server draft`, `Needs review`, `Published`, `Assigned`, `Archived`, and `Conflict`.

## Phase 3 Foundation Summary

- Added `src/lib/learning/objects.ts` with `LearningObject`, `LearningBlock`, `LearningWorkflowState`, source types, block types, state labels, and normalization helpers.
- Added adapters for legacy Studio documents, lesson sections, quiz questions, and content blocks.
- Added focused tests in `src/lib/learning/objects.test.ts` for state labels, tag normalization, Studio conversion, lesson section conversion, quiz conversion, content block conversion, and composed legacy objects.
- Added `src/lib/learning/lesson-package.ts` to compose legacy lesson rows, lesson sections, and quiz questions into a package-level `LearningObject`.
- Added `src/lib/learning/lesson-package.test.ts` for package composition and UI summary counts.
- Updated Studio's saved block library to use shared learning-object state labels and block type mapping.
- Updated the teacher lesson editor header to summarize the current lesson through `lessonRowsToLearningObject`, including shared state, block count, and package duration.
- Phase 3 remains in progress until large teacher/student lesson pages consume the shared layer and duplicated section/quiz normalization is removed.

## Phase 22 Schema Audit Summary

- Added `docs/DATA_SCHEMA_AUDIT.md` with the current D1 schema grouped by identity/auth, tenancy, lessons, Studio, work/grades, engagement, media/security, AI, automation, analytics, billing, and offline sync.
- Verified the schema against `src/lib/db/schema.ts`, the tenant helpers, D1 REST adapter, learning event helpers, and key APIs for Studio, work, submissions, grades, billing, catalog, content blocks, standards, practice, and generic data access.
- The strongest current pattern is the tenant-scoped API layer for newer features plus `learning_events` as the future replay/audit spine.
- The main architecture risks are broad legacy `edsync.from(...)` reads/writes, generic allowed-table reads through `/api/data`, implicit default tenant membership creation, REST-only D1 access on Cloudflare runtime, and gradebook materialization that is not fully replay-derived yet.
- Recommended next order: explicit active tenant context, feature-owned APIs for teacher/student lesson/dashboard flows, tenant enforcement for legacy lesson/progress records, Studio as canonical authoring source, hot-path indexes, grade replay service, Worker-native D1 adapter, and a richer offline queue/replay UX.
- Added `database/migrations/0008_hot_path_indexes.sql` with additive indexes for lesson dashboards, lesson ordering, quiz ordering, student assignments, progress lookups, work submissions, catalog products, tenant object links, and notification lists.
- Applied the migration set to remote Cloudflare D1 database `edsync-dev-d1`; the final hot-path index migration completed successfully.
- Added a D1 query adapter boundary so Cloudflare Workers can use the native `EDSYNC_DB` binding while Vercel/local/Docker keep the existing REST fallback.
- Deployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `cb7a1cb3-4d42-4c3e-a4fb-f1dc8b75422c`.
- Added an HTTP-only `edsync_active_tenant` cookie for organization login/signup, clear it on logout, and made tenant resolution prefer a validated active tenant after custom-domain routing.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9375ee85-c8d1-40b1-bd38-905ff76b3319`.

## Public Intro Redesign Summary

- Replaced the word-heavy catalog intro with a compact launch-style hook and animated product-preview deck.
- Added a horizontal product gallery that previews Studio, teacher workflow, student practice, admin command, and discussion surfaces.
- Tightened mobile header behavior so the public topbar keeps theme/language controls visible without clipping.
- Added responsive motion and overflow safeguards for desktop and mobile.
- Split the workflow showcase into a dedicated interactive component with sticky overlay-style scrolling, clickable slide dots, previous/next controls, and concrete screen previews for Studio, AI, teacher dashboard, student practice, admin command, and discussion.
- Refined the intro again after live browser review: the hook now maps to the real EdSync spine, the first preview shows catalog, organization, Studio, practice, admin, assignment, AI review, and grade-event states without clipping, and the scroll bridge now previews actual route handoffs instead of generic labels.
- Verification for this pass: local Chrome screenshots covered light desktop, dark desktop, mobile, workflow bridge, and the dedicated showcase page with no horizontal overflow. `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed.
- Refined the intro/workflow connection so `View workflow` stays on the catalog page, the product preview is larger and clearer, organization/teacher labels no longer collide, and workflow gallery controls change slides in place instead of scrolling the page.
- Browser verification for this pass confirmed dot navigation changes active slides with `jump: 0`, autoplay advances slides without moving scroll, and desktop/mobile intro views have no horizontal overflow. `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed.
- Reworked the public intro/workflow style closer to the LEARN reference: transparent launch header controls, dark cinematic workflow stage, left narrative panel, realistic app window preview, and bottom tab navigation for Dashboard/Studio/AI/Practice/Admin/Catalog style slides.
- Verification for this pass: local browser smoke confirmed no horizontal overflow, transparent public header in dark mode, in-place workflow tab switching with no scroll jump, and successful `typecheck`, `lint`, `test`, and `build`.
- Replaced the public catalog topbar on `/catalog` and `/showcase` with a floating launch chrome, rebuilt the hero around a larger concrete EdSync workspace preview, set the public launch surface to dark-first, reordered workflow slides around Catalog, Studio, AI, Review, Practice, and Progress, and made language selection fall back to clean English public copy while preserving full language names.
- Verification for this pass: local in-app browser QA confirmed no conventional public topbar, dark/light toggle persistence, language menu full-name options, in-place workflow tab switching, and no desktop horizontal overflow. Headless Chrome desktop/mobile screenshots were reviewed; mobile headless Chrome appears to enforce a wider CSS layout than the captured bitmap, so final live smoke should still check a real narrow browser viewport after deploy.
- Continued public launch polish on 2026-05-17: tightened the workflow story into shorter product-specific slide copy, replaced the workflow mini nav with a compact auto-playing gallery label, widened the app preview area, shortened the intro-to-workflow bridge, and moved mobile theme/language/sign-in controls into a compact top-right cluster.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local desktop, mobile, and workflow screenshots were reviewed after restarting the Next dev server.
- Continued public language and performance polish on 2026-05-17: replaced mojibake public translations with clean UTF-8 copy, made `/catalog?language=...` render localized public copy, made the language menu update the catalog language query, split lightweight language metadata away from the full translation table to keep the client bundle small, and tuned mobile hero type for longer translated hooks.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Build output kept `/catalog` at `104 kB` first-load JS, and local Spanish desktop/mobile screenshots plus workflow-anchor screenshots were reviewed.
- Continued screenshot-directed public launch fixes on 2026-05-18: removed the separate visitor topbar from `/catalog`, moved EdSync brand/actions/theme/language/direct jumps into the hero line area, removed the public-catalog watermark/status copy, replaced the hero browser frame with an open EdSync workspace preview, compacted the function tags, merged the workflow intro into slide 1, and combined catalog search/results into one availability surface.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3300/catalog` returned 200, and headless Chrome desktop/mobile screenshots were reviewed for hero spacing, mobile controls, preview contrast, and no clipped top controls. Remaining visual risk: hash screenshots for sticky workflow/search anchors in headless Chrome are unreliable, so final deployed browser smoke should still check real scroll behavior manually.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `4b886979-2340-4c55-91b6-64ff2e0d4d4d`. Live smoke checks returned 200 for `/catalog` and `/showcase`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public launch refinement on 2026-05-18: added discrete wheel/touch workflow slide stepping while preserving normal page scroll, reduced workflow height and the workflow-to-catalog gap, tightened mobile hero/preview spacing, and cropped the mobile preview so the first viewport stays cleaner.
- Verification for this pass: initial parallel typecheck raced with `.next` regeneration and failed on missing generated `.next/types` files; after `npm.cmd run build` regenerated `.next`, sequential `npm.cmd run typecheck` and `npm.cmd run lint` passed. `npm.cmd run test` and `npm.cmd run build` also passed. Local production preview on `http://127.0.0.1:3400/catalog` returned 200, and desktop/mobile headless Chrome screenshots were reviewed.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9969daee-461d-490a-a7b0-3475d5b625d0`. Live smoke checks returned 200 for `/catalog` and `/showcase`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public launch i18n/showcase polish on 2026-05-18: added lightweight localized language-menu labels, made `/catalog?language=...` drive the hero controls as well as the main headline/body, shortened long translated hero hooks, added language-specific hero sizing for Korean/Khmer/Thai, and removed the leftover public topbar chrome from `/showcase`.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3500/catalog?language=Korean` returned 200, and desktop/mobile Korean screenshots plus `/showcase` screenshots were reviewed for localized controls, no topbar, and better mobile headline scale.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `696d27fd-d722-4208-a9eb-a5427bb72eb3`. Live smoke checks returned 200 for `/catalog`, `/catalog?language=Korean`, and `/showcase`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public launch refinement on 2026-05-19: localized the hero preview mockup labels for public languages, including the preview nav, lesson studio status, slide label, linked-media line, loop actions, and metric labels. Simplified the mobile preview by hiding the mini preview rail and extra loop/proof rows so the first phone viewport reads cleaner.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3501/catalog?language=Korean` returned 200, and desktop/mobile Korean screenshots were reviewed for localized preview labels and reduced mobile clutter.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c79ac9bd-385c-4125-bbd9-5f7e2d6df033`. Live smoke checks returned 200 for `/catalog?language=Korean` and `/showcase`, and `/api/catalog` returned an empty catalog payload without errors.
- Added Phase 23 to `plan.md` for source organization, folder grouping, compatibility shims, runtime language strategy, and cleanup. Decision: keep TypeScript/React as the primary request-path language for Next.js/Cloudflare/Vercel compatibility; consider Rust/WASM only for profiled CPU-heavy isolated utilities such as standards parsing, document conversion, media signature checks, or large import normalization.
- Began Phase 23 with a safe public helper grouping slice: moved public language metadata to `src/lib/public/languages.ts`, moved public copy/i18n data to `src/lib/public/i18n.ts`, updated public UI/tests to import from the grouped paths, and removed the old flat shim files after `rg` confirmed no references remained.
- Verification for this organization pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Build output kept `/catalog` at `105 kB` first-load JS, so the grouping did not add public bundle weight.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `23403c89-5101-4a2a-a268-4b79e286af47`. Live smoke checks returned 200 for `/catalog` and `/showcase`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued workspace shell refinement on 2026-05-19: removed misleading hardcoded sidebar markers such as fake counts and the Studio `S`, wrapped shared tool routes (`/studio`, `/ai`, `/practice`, `/notes`, `/docs`, `/sheets`, `/slides`, `/quizzes`, `/games`) in the role-aware workspace shell, made protected mobile auth redirects narrower and icon-only, quieted empty notification status, added role-aware shell accent color, added desktop gutters between sidebar and page content, and replaced Studio's fake unsaved count with a draft dot.
- Verification for this shell pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3502/auth/login` returned 200, and a mobile headless Chrome screenshot confirmed the login card and theme/language icons no longer clip.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `4d1310c1-87ed-4eaf-aabb-95a21c15713e`. Live smoke checks returned 200 for `/catalog` and `/auth/login`, `/studio` returned the expected unauthenticated 307 redirect, and `/api/catalog` returned an empty catalog payload without errors.
- Continued shell responsiveness on 2026-05-19: compacted the protected mobile header to keep the brand icon, theme, language, and notifications from crowding narrow screens; clamped the mobile drawer to the viewport; and restored focused admin Governance links for Standards, Certifications, and Automation so those pages remain visibly connected in the sidebar.
- Verification for this follow-up: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3bf2f508-dcc4-4c92-b9d4-9463c3bfde6b`. Live smoke checks returned 200 for `/catalog` and `/auth/login`, `/admin/dashboard` returned the expected unauthenticated 307 redirect, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public launch refinement on 2026-05-19: replaced the static hero mockup with an auto-playing, dot-controlled EdSync preview gallery for Catalog, Studio, AI, Practice, and Proof/Admin surfaces; each slide now uses concrete route labels and app-like panels for catalog products, lesson slides, AI output, timed practice, and grade evidence. Tightened small-screen public launch controls so EdSync, theme, language, and sign-in remain on one compact line, and simplified the mobile preview rail to keep the first viewport cleaner.
- Verification for this launch gallery pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3504/catalog` returned 200, and desktop/mobile headless Chrome screenshots were reviewed for gallery dots, auto-advance-ready layout, compact controls, and no clipped top actions.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `eb9aa90f-e770-4f95-a55d-5dae522538dd`. Live smoke checks returned 200 for `/catalog`, `/showcase`, and `/auth/login`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued gallery interaction cleanup on 2026-05-19: hero preview dots and side preview buttons now pause autoplay after manual interaction or focus/hover so the gallery does not immediately jump away from the user-selected slide; selected dots expose `aria-current`, and stale unused workflow bridge props were removed from `/showcase` and the shared workflow component.
- Verification for this cleanup pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d35f1e6f-3e85-4333-bc4c-0c8d335666d2`. Live smoke checks returned 200 for `/catalog`, `/showcase`, and `/auth/login`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued mobile gallery refinement on 2026-05-19: added swipe gestures and left/right keyboard support to the hero preview gallery, disabled autoplay for `prefers-reduced-motion`, and added a polished focus-visible state for keyboard users.
- Verification for this interaction pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3505/catalog` returned 200, and desktop/mobile screenshots were reviewed for compact controls, readable gallery content, and no clipped launch actions.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `eb6ad0ce-010f-4df7-9f41-9de7d67a06dd`. Live smoke checks returned 200 for `/catalog`, `/showcase`, and `/auth/login`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public i18n refinement on 2026-05-19: changed the launch preview gallery to accept server-provided preview copy, kept a compatibility path for existing labels, and wired the catalog hero so preview nav labels, ready/start state, key metrics, and compact block labels reuse the selected public language strings instead of staying fully English.
- Verification for this i18n pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3506/catalog?language=Spanish` returned 200, and a mobile screenshot was reviewed for localized hero text, compact controls, and localized preview labels without clipping.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3ba9584b-dc82-4424-b759-907469f4ab60`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase`, and `/auth/login`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public preview localization on 2026-05-19: reduced the remaining English-heavy hero preview titles and block summaries by using selected-language public labels for Catalog, Studio, AI, Practice, Grades, Courses, Search, Filters, Duration, Difficulty, Free, and Start across the gallery.
- Verification for this preview label pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3507/catalog?language=Spanish` returned 200, and a mobile screenshot confirmed the preview uses shorter localized labels without clipping.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `08d9b1d7-ca81-4a45-b251-37ad555e3383`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase`, and `/auth/login`, and `/api/catalog` returned an empty catalog payload without errors.
- Continued public workflow localization/responsiveness on 2026-05-19: made the workflow showcase consume selected-language public labels, passed catalog/showcase language query values into the workflow, replaced the remaining public intro divider line, compressed the mobile hero/workflow copy, and changed narrow workflow side panels into compact horizontal chips to reduce overflow risk.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed when run sequentially. A parallel typecheck/build attempt hit the known `.next/types` generation race, then standalone `typecheck` passed after build regeneration. Local production preview on `http://127.0.0.1:3509/catalog?language=Spanish` returned 200, and desktop/mobile/workflow screenshots were reviewed for localized labels, no intro divider line, compact controls, and no visible horizontal overflow.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `dc759a07-1acf-44a7-82a9-6fe52e7a0c02`. Live smoke checks returned 200 for `/catalog?language=Spanish` and `/showcase?language=Spanish`; `/api/catalog` initially had a transient connection failure, then retried successfully with `{"data":{"items":[],"portals":[]},"error":null}`.
- Continued public mockup cleanup on 2026-05-19: removed the old English fallback story from the launch preview gallery, tightened the workflow mockup labels away from specific fake classroom text, added localized utility labels for media/provider/review/security/health, and reduced remaining demo wording in the Spanish workflow view. Remaining i18n depth: technical product terms such as quiz/rubric/slides can be moved into the public copy table in a later pass.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Local production preview on `http://127.0.0.1:3510/catalog?language=Spanish` returned 200, and mobile/workflow screenshots were reviewed for compact layout, localized Progreso/Medios/Proveedor/Revisión labels, and no horizontal overflow.
- Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `fb9583b7-3e06-4e7d-a4ac-21b379ec83e5`. Live smoke checks returned 200 for `/catalog?language=Spanish` and `/showcase?language=Spanish`; `/api/catalog` initially had a transient connection failure, then retried successfully with `{"data":{"items":[],"portals":[]},"error":null}`.
- Continued public workflow localization on 2026-05-19: expanded workflow mockup utility labels for route, events, audit, slides, quiz, rubric, retry, scoring, submissions, media checks, and admin terms across the supported public languages; replaced the remaining hardcoded English fragments in the workflow slides with those labels.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `2e2c2a4d-1cfe-40c8-8855-680a95723ad1`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public catalog localization on 2026-05-19: made catalog cards accept localized labels, passed selected-language labels into global and organization catalog cards, localized the global catalog quick-search chips, removed English-only launch preview metric labels, localized organization catalog filter controls, and fixed the product detail duration copy so flexible courses no longer render as "Flexible minutes."
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `1552f9e9-10a4-49a0-9701-3ff037378bda`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public org/detail cleanup on 2026-05-19: shortened and localized organization portal hero copy, organization CTAs, side panels, and empty states using existing public language labels; made catalog product detail read selected-language labels for catalog return, duration, difficulty, language, enrollment/status blocks, organization link, and the safe-preview note.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `a234e9d8-c7c1-42a4-b71b-8bdb660bc833`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public language-flow cleanup on 2026-05-19: catalog cards now preserve the selected language in course-detail links, free course price badges use localized free labels, global and organization catalogs pass language into cards, and product detail reads the language query or language cookie before linking back to catalog or organization pages.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d3ef6886-86cc-40a6-b226-aa675ed79649`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public navigation continuity on 2026-05-19: added catalog URL helpers so clear-filter links, quick-search chips, category chips, academy cards, organization clear links, and empty-state back-to-catalog links preserve the selected public language instead of silently returning to English.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5f60e41e-0f30-4325-92d7-e45e9554e6e4`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public enrollment localization on 2026-05-19: made `CatalogEnrollButton` accept localized labels and language, localized free price display on product detail, passed selected-language labels into the enroll button, and preserved the language query through enrollment login, success, and cancellation redirects.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d21618d4-a61f-4cb9-b194-2c1e8d22a5de`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public auth-link localization on 2026-05-19: launch hero sign-in/start links, the reusable public topbar, organization portals, and catalog detail pages now preserve the selected public language when sending visitors into login/signup or workspace actions.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9e370153-ad33-4bda-aa24-b11a73664a80`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public auth language handoff on 2026-05-19: the shared language menu now reads `?language=` on any route, persists that preference to local storage/cookies, updates `<html lang>`, and login/signup now preserve the selected language through auth links, signup redirects, organization portal links, and localized public action labels.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `470ba0e4-9a53-46e9-aa34-0293b501db51`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/login?language=Spanish`, `/auth/signup?language=Spanish`, and `/api/catalog`.
- Continued auth localization depth on 2026-05-19: added a focused public auth copy layer and connected login/signup organization controls, account type cards, role cards, form labels, status messages, loading labels, and auth cross-links to selected-language copy with English fallbacks.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `875e5255-9fe4-4114-84a1-60506fa3dc71`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/login?language=Spanish`, `/auth/signup?language=Spanish`, `/auth/login?language=Korean`, and `/api/catalog`.
- Continued auth flow localization completion on 2026-05-19: expanded auth labels for validation/success states, organization-found summaries, email confirmation text, side-panel copy, and organization-benefit copy; login/signup now use those labels for toasts, panel text, and status messages instead of reverting to English mid-flow.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `98488cd1-5710-4e14-94d4-6fe686b3ffdb`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/login?language=Spanish`, `/auth/signup?language=Spanish`, `/auth/login?language=Korean`, and `/api/catalog`.
- Continued public language URL cleanup on 2026-05-19: auth language state now initializes from stored preferences before first paint when no query language is present, and launch/topbar auth links omit redundant `?language=English` while preserving non-default languages.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `015f3425-25ca-4bf4-ae68-e5986300e45f`. Live smoke checks returned 200 for `/catalog`, `/auth/login`, `/catalog?language=Spanish`, `/auth/login?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public i18n hardening on 2026-05-19: added a shared `publicLanguageQuerySuffix` helper, reused it in the auth language hook and launch hero, and strengthened `public-i18n` tests to assert readable Spanish/Korean copy, auth fallback behavior, and default-language URL omission.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed; public i18n coverage now reports 65 total tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `f76c8419-a492-4305-a90e-eecd46f5cbbd`. Live smoke checks returned 200 for `/catalog`, `/auth/login`, `/catalog?language=Spanish`, `/auth/login?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued catalog language URL consolidation on 2026-05-19: reused the shared public language query helper in catalog cards, the enrollment button, catalog detail links, organization portal links, and enrollment API redirects so default English stays clean while non-default languages remain consistent through public browsing and login/enrollment handoff.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed; focused `public-i18n` coverage also passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `e2a0bea1-96e5-4630-8df3-acc31dd55c7f`. Live smoke checks returned 200 for `/catalog`, `/auth/login`, `/catalog?language=Spanish`, `/auth/login?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public catalog link normalization on 2026-05-19: added a reusable public language query value helper, used it in catalog quick/search/category/academy links, and kept organization links on the shared suffix helper so English no longer propagates as a noisy public query while supported non-default languages stay intact.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 65 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5391355d-7eda-4f6e-a904-ca8901b5df41`. Live smoke checks returned 200 for `/catalog`, `/catalog?language=English`, `/catalog?language=Spanish`, `/auth/login?language=Spanish`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public catalog filter cleanup on 2026-05-19: split public UI language from catalog course-language filtering by adding `courseLanguage`, preserving the selected UI language through catalog and organization search forms, and keeping the catalog API backward-compatible with legacy `language` course filters while the UI uses `language` only for public copy.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `435232b3-6696-45ca-820d-71409f498130`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/catalog?language=Spanish&courseLanguage=English`, `/api/catalog?courseLanguage=English`, `/api/catalog?language=English`, `/showcase?language=Spanish`, and `/auth/login?language=Spanish`.
- Continued public auth CTA continuity on 2026-05-19: preserved selected public language through empty-catalog workspace creation, organization portal sign-in/sign-up CTAs, the legacy launch chrome login action, and the standalone workflow back-to-intro link so the visitor flow no longer drops back to English at these entry points.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `6a56912f-df64-4eef-a4d3-f735be5f18a6`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/signup?language=Spanish`, `/auth/login?language=Spanish`, and `/api/catalog`.
- Continued public URL helper consolidation on 2026-05-19: added a reusable `publicLanguageHref` helper and moved public topbar, launch hero, legacy launch chrome, organization auth CTAs, catalog empty-state signup, and workflow return links onto the shared helper so language-aware public links are easier to keep consistent.
- Verification for this pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests; focused public i18n tests cover the new helper. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `588bd053-c548-4c79-8986-ee11b615b553`. Live smoke checks returned 200 for `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/signup?language=Spanish`, `/auth/login?language=Spanish`, and `/api/catalog`.
- Continued public surface cleanup on 2026-05-19: removed the unused legacy `landing.tsx` surface and the unused `PublicLaunchChrome` component so the codebase no longer carries older topbar-style public UI that conflicts with the current LEARN-style launch/catalog experience.
- Verification for this cleanup pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `18bf4013-4918-4e37-b586-6f24554b716f`. Live smoke checks returned 200 for `/catalog`, `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/login?language=Spanish`, and `/api/catalog`.
- Continued public CSS cleanup on 2026-05-19: removed stale `.edsync-launch-chrome`, `.edsync-launch-brand`, and `.edsync-launch-actions` rules and mobile overrides left behind by the deleted legacy launch chrome while preserving the shared mark/icon/sign-in styles used by the current inline launch header.
- Verification for this stylesheet pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0b1a2574-d14f-451b-b01f-4165c5ca3311`. Live smoke checks returned 200 for `/catalog`, `/catalog?language=Spanish`, `/showcase?language=Spanish`, `/auth/login?language=Spanish`, and `/api/catalog`.
- Continued root public entry cleanup on 2026-05-19: expanded the root catalog search parameter shape to include price, category, difficulty, public language, course language, and duration, and preserved selected public language when root-domain tenant resolution redirects visitors into an organization portal.
- Verification for this root pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `e76d72b6-dbee-4796-8816-887b4246ccdc`. Live smoke checks returned 200 for `/`, `/?language=Spanish`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued catalog parameter typing cleanup on 2026-05-19: moved the public catalog/root search parameter shape into `CatalogSearchParams` in `catalog-filters`, and reused it from `/` and `/catalog` so language, course-language, and filter routing stay aligned without duplicate page-local types.
- Verification for this typing pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `b147c578-a9af-471f-a4b4-2c8939ee9e7f`. Live smoke checks returned 200 for `/`, `/?language=Spanish`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued public route typing cleanup on 2026-05-19: reused the shared catalog search type for organization portals and added a shared public-language search type for showcase and catalog detail pages, reducing one-off public route shapes while preserving current URLs.
- Verification for this route-typing pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 66 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `e3e26ffe-3c78-4702-9098-f42491c708ff`. Live smoke checks returned 200 for `/`, `/?language=Spanish`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`.
- Continued Practice runtime hardening on 2026-05-19: added a shared practice-mode validator, normalized `/practice?mode=` before rendering the workspace, and rejected unsupported practice attempt modes in the API so malformed URLs or payloads cannot create invalid attempt records.
- Verification for this Practice pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 69 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `78518b31-aa5d-437e-84ca-9212590425aa`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; protected `/practice?mode=sprint` and `/practice?mode=bad-mode` both redirected to auth as expected.
- Continued AI prompt route hardening on 2026-05-19: added a shared AI prompt task normalizer, reused it on `/ai?task=`, and covered valid, invalid, and fallback task ids so malformed prompt links open the default guided workflow instead of carrying arbitrary task state.
- Verification for this AI route pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 72 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `1e18ede0-e083-4b9c-b9cf-2b4143854fd3`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; protected `/ai?task=generate-practice` and `/ai?task=bad-task` both redirected to auth as expected.
- Continued Studio API hardening on 2026-05-19: added server-side Studio document status validation and reused it in Studio create/update writes so only `draft`, `published`, and `archived` can be stored, with archived blocked on create and publishing still permission-guarded.
- Verification for this workspace status pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 73 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3eaba12f-aa33-45cc-8569-f187a3c741d5`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/studio` returned 401 as expected.
- Continued content block API hardening on 2026-05-19: added explicit content-block status validation and reused it in create/update writes so invalid statuses no longer silently downgrade to `draft`; archived remains blocked on create while publish checks remain permission-gated.
- Verification for this content-block status pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 74 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0321be8a-88f1-44a0-bdee-1a1dd33b8e75`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/content-blocks` returned 401 as expected.
- Continued work-item API hardening on 2026-05-19: added shared work validation for work type, status, and point bounds, then reused it in work item creation so unsupported types/statuses fail clearly and point values stay finite and bounded before D1 writes.
- Verification for this work-item validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 77 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `562193c4-a45f-468a-907b-5addb59f4661`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/work` returned 401 as expected.
- Continued grading API hardening on 2026-05-19: extended work validation to bound earned points by possible points and reused it in work submission grading so grade events cannot persist negative, non-finite, or over-maximum score values.
- Verification for this grading validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 78 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `353f1f72-a44d-4cd4-ac10-e65ac72ddd62`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/work/submissions` returned 401 as expected.
- Continued submission payload hardening on 2026-05-19: added a bounded submission-response validator and reused it in work submission creates so student responses must be JSON objects and stay under the configured size limit before they are stored in D1.
- Verification for this submission payload pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 79 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `31d1b23a-cb40-4f5a-8d0d-bd8668a31a7f`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/work/submissions` returned 401 as expected.
- Continued discussion API hardening on 2026-05-19: added shared discussion text validation and reused it in discussion thread/post creation so titles, prompts, and post bodies are trimmed, required when appropriate, and bounded before D1 writes.
- Verification for this discussion validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 82 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `31e860ec-b541-4c06-a611-5fb2f710ea2d`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/discussions` returned 401 as expected.
- Continued student-note API hardening on 2026-05-19: added shared student-note validation and reused it in teacher note creation so note titles and bodies are trimmed and bounded, note visibility is normalized to safe values, and notification priority is reused from the existing notification validator.
- Verification for this student-note pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 85 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `502673ce-4678-4b5b-bc36-645632248edd`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/discussions` and `/api/notes` returned 401 as expected.
- Continued gradebook API hardening on 2026-05-19: added shared grade validation for category names, category weights, manual score titles, source identifiers, feedback text, score percentages, and earned/possible point bounds, then reused it in manual grade and lesson quiz grade recording.
- Verification for this gradebook pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 89 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5d239ff9-13ee-41f2-b639-44d8dc28bd58`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated `/api/grades` returned 401 and POST `/api/grades/lesson-quiz` returned 403 as expected.
- Continued offline sync hardening on 2026-05-19: added shared offline-sync validation for batch size, client ids, item ids, item types, and payload size/shape, then reused it in `/api/offline-sync` before append-only learning events are recorded.
- Verification for this offline sync pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 93 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `df6001b8-68ab-45db-b827-a74450e4397b`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; GET `/api/offline-sync` returned 405 and unauthenticated POST `/api/offline-sync` returned 401 as expected.
- Continued learning-event API hardening on 2026-05-19: added shared learning-event validation for source identifiers, event identifiers, optional source ids, and payload size/shape, then reused it in `/api/events` before rows are appended to the tenant event log.
- Verification for this learning-event pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 97 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `2c2916ae-496d-48b3-b8fe-f86cafc4c36f`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/events` returned 401 as expected.
- Continued practice review hardening on 2026-05-19: added shared review-card validation for ids, mastery states, and optional next-review dates, then reused it in `/api/reviews` for update and delete operations before spaced-review records are mutated.
- Verification for this practice review pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 101 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `866de799-c168-4d56-9bb3-9471583ab1cb`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and PATCH `/api/reviews` returned 401 as expected.
- Continued admin settings hardening on 2026-05-19: added shared feature-flag validation for ids, keys, labels, descriptions, enabled state, and audiences, then reused it in `/api/admin/settings` for flag toggles, create, update, and delete operations.
- Verification for this admin settings pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 105 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `14ce0777-dfeb-46ec-9515-d316ac94c76c`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and PATCH `/api/admin/settings` returned 401 as expected.
- Continued permission profile hardening on 2026-05-19: added shared tenant role-profile validation for ids, labels, descriptions, profile keys, and allowed permission keys, then reused it in `/api/permissions` for create, update, and delete operations with explicit JSON serialization for stored permissions.
- Verification for this permission profile pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 109 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c2a6074c-ea25-4b8c-8c68-2717d2c97dc8`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/permissions` returned 401 as expected.
- Continued tenant creation hardening on 2026-05-19: added shared tenant validation for organization names, slugs, plan tiers, and D1 isolation modes, then reused it in `/api/tenants` before creating tenant, portal, and owner membership records.
- Verification for this tenant validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 113 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c4ca2ba0-759c-457f-8f2e-7c1d4459ce5a`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/tenants` returned 401 as expected.
- Continued portal management hardening on 2026-05-19: added shared portal validation for ids, names, slugs, audiences, domains, and catalog settings, then reused it in `/api/portals` for create, update, delete, make-default, and public-toggle operations with explicit JSON serialization for portal catalog settings.
- Verification for this portal validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 118 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `fecd3db2-c120-4be2-95d7-b92170cf1347`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/portals` returned 401 as expected.
- Continued billing/catalog hardening on 2026-05-19: added shared billing validation for product ids, price ids, titles, descriptions, product types, statuses, course/portal ids, currencies, amounts, intervals, and checkout URLs, then reused it in `/api/billing` while explicitly serializing catalog metadata before direct D1 writes.
- Verification for this billing validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 123 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `03bcc67f-617c-4440-a500-4db88f722c7b`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/billing` returned 401 as expected.
- Continued certification-rule hardening on 2026-05-19: extended certification validation to cover rule ids and optional course ids, reused it in `/api/certifications` for update/delete payloads, and explicitly serialized certification settings before direct D1 writes.
- Verification for this certification validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 124 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `517f5e87-930b-4492-ad7a-8039a902e3c3`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/certifications` returned 401 as expected.
- Continued automation-rule hardening on 2026-05-19: extended automation validation to cover rule ids, reused it in `/api/automation-rules` for delete/toggle/update payloads, and explicitly serialized seeded and user-edited automation conditions/actions before direct D1 writes.
- Verification for this automation validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 124 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `91f7d046-f09e-46ec-b221-2ce96dacc2d5`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/automation-rules` returned 401 as expected.
- Continued email/outbox hardening on 2026-05-19: extended shared email validation to cover optional HTML bodies, class/record ids, and bounded metadata objects; reused those checks in queued email normalization and `/api/email/send` so provider/outbox messages cannot carry script-like HTML, loose class ids, or oversized metadata.
- Verification for this email validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 125 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `a9f36d47-78b2-46bb-8322-642b601e6a3a`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET `/api/email/send` returned 405 and unauthenticated POST `/api/email/send` returned 401 as expected.
- Continued notification hardening on 2026-05-19: extended shared notification validation to cover target/notification/actor ids, action URL length, and bounded metadata objects; reused those checks in notification queue creation and `/api/notifications` for create, mark-read, and delete operations.
- Verification for this notification validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 126 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `7875f125-03a0-4f58-a00e-85d8149db97f`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/notifications` returned 401 as expected.
- Continued lesson-assignment notification hardening on 2026-05-19: added shared validation for lesson/class/student ids and assignment notification payloads, then reused it in `/api/notifications/lesson-assigned` so all assignment notification lookups use normalized ids and validated due dates before touching lesson, class, roster, notification, or email flows.
- Verification for this lesson-assignment notification pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 128 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `b40da637-6e22-423a-a7ce-0e679a976aaa`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET `/api/notifications/lesson-assigned` returned 405 and unauthenticated POST `/api/notifications/lesson-assigned` returned 401 as expected.
- Continued admin user-management hardening on 2026-05-19: added shared admin user validation for search text, user ids, and explicit admin access booleans, then reused it in `/api/admin/users` so global admin grants/revokes cannot be triggered by malformed ids or missing boolean payloads.
- Verification for this admin user validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 132 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `741a9abf-b787-444f-8159-410a5c1b174a`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and PATCH `/api/admin/users` returned 401 as expected.
- Continued admin settings hardening on 2026-05-19: feature flag create/update/toggle payloads now require an explicit boolean enabled state instead of relying on JavaScript truthiness, preventing strings such as `"false"` from enabling platform flags by accident.
- Verification for this feature flag validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 133 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d10272f0-66ba-46df-821f-be69e15ce9a6`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and PATCH `/api/admin/settings` returned 401 as expected.
- Continued automation rule hardening on 2026-05-19: automation create/update/toggle payloads now require explicit boolean enabled values instead of string/truthy coercion, while new automation rules still default to enabled when the field is omitted.
- Verification for this automation validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 134 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `74595c8a-1c9b-4cdd-a4b1-d816394737ef`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/automation-rules` returned 401 as expected.
- Continued certification rule hardening on 2026-05-19: certification expiry and renewal-notification timing now reject non-numeric payloads instead of silently falling back to default values, while still clamping valid numeric values into safe ranges.
- Verification for this certification validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 134 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `f7bffca9-70ad-4823-b60b-28402ca97db2`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; unauthenticated GET and POST `/api/certifications` returned 401 as expected.
- Continued organization entry hardening on 2026-05-19: added bounded organization-code validation for public organization lookup so malformed or overlong organization codes are rejected before D1 queries, while normalized organization slugs remain supported for sign-in/start flows.
- Verification for this organization lookup validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 136 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `ec12fc0a-2a17-4c6f-8350-5b84136cc97d`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, `/api/catalog`, and `/api/auth/organizations?code=edsync`; an overlong organization code returned 400 as expected.
- Continued organization auth hardening on 2026-05-19: organization login and organization-join signup now reuse the bounded organization-code validator, so the auth paths reject missing, malformed, or overlong organization codes before membership lookup or account creation work.
- Verification for this organization auth validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 136 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `510a2cde-8d39-41b3-9d53-768074878074`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, `/api/catalog`, and `/api/auth/organizations?code=edsync`; overlong organization codes returned 400 for both `/api/auth/login` and `/api/auth/signup` as expected.
- Continued email boundary hardening on 2026-05-19: shared email validation now enforces a 254-character maximum address length across reply-to, direct recipients, queued email, and deduplicated recipient lists.
- Verification for this email address validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 136 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d9a8bee0-779e-477a-9d35-e6dbc76e0264`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; GET `/api/email/send` returned 405 and unauthenticated POST `/api/email/send` returned 401 as expected.
- Continued storage upload hardening on 2026-05-19: added bounded object-path validation for storage bucket aliases and upload paths, limiting sanitized R2 object paths by total length and segment count before file buffers are read, scanned, or persisted.
- Verification for this upload path validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 136 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c0bea4e1-eadf-4c6b-9514-72ce471fe187`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; GET `/api/storage/upload` returned 405 and unauthenticated POST `/api/storage/upload` returned 401 as expected.
- Continued catalog media hardening on 2026-05-19: public media URL sanitization now enforces a 2048-character maximum before URL parsing, catalog metadata mapping, image display, direct video links, or approved embed normalization.
- Verification for this media URL validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 136 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `450c89c4-f9ac-41f8-b63e-3e581002d13a`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; `/catalog/nonexistent-product` returned 404 as expected.
- Continued catalog identifier hardening on 2026-05-19: added shared catalog product id validation and reused it across public catalog item API, catalog enrollment API, and catalog detail page metadata/rendering so malformed product ids fail before database lookups.
- Verification for this catalog id validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 137 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `7f40019d-2d96-4afe-b763-a03ce72a4e55`. Live smoke checks returned 200 for `/`, `/catalog?language=Spanish&courseLanguage=English`, `/showcase?language=Spanish`, and `/api/catalog`; `/api/catalog/nonexistent-product` returned 404, malformed catalog API/enroll ids returned 400, and an overlong catalog detail id returned 404 as expected.
- Continued public language routing on 2026-05-19: extracted the public language URL sync logic so language changes now preserve filters while applying to `/`, `/catalog`, `/org/*`, and `/showcase` instead of only catalog/org routes.
- Verification for this language routing pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 139 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `a4e4f2aa-a6a3-43ab-9536-3ea5a072e394`. Live smoke checks returned 200 and expected localized copy for `/`, `/?language=Spanish`, `/catalog?language=French`, `/showcase?language=Vietnamese`, and `/api/catalog`.
- Continued public language bootstrap hardening on 2026-05-19: the root layout preference script now uses the shared EdSync public language list and honors the `language` query parameter before stored preferences, so direct localized public links update `<html lang>` correctly in the browser.
- Verification for this language bootstrap pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 142 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `f4b97d82-ca0a-4c68-81c2-4fbbacee60e5`. Live smoke checks returned 200 for `/`, `/?language=Spanish`, `/catalog?language=Korean`, `/catalog?language=Thai`, and `/api/catalog`; the live bootstrap script set `document.documentElement.lang` to `ko` for `?language=Korean`.
- Continued public chrome refinement on 2026-05-19: the shared public product/org chrome is now non-sticky, more compact on small screens, and keeps the sign-in/start action visible beside theme and language controls instead of hiding the primary action on mobile.
- Verification for this public chrome pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 142 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `6b3f05e8-241e-46f5-a1c6-56f89270b0bb`. Live smoke checks returned 200 for `/org/edsync`, `/catalog?language=Spanish`, and `/api/catalog`; `/catalog/nonexistent-product` returned 404 as expected.
- Continued shared workspace shell hardening on 2026-05-19: Studio, AI, practice, notes/docs/sheets/slides, quizzes, and games now resolve the app shell role from the server session instead of trusting only the readable role cookie or falling back to the student shell.
- Verification for this shared shell pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 142 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5b34818d-cdd2-4790-8bc6-85f58a60ff90`. Live smoke checks returned 307 redirects with preserved `next` parameters for unauthenticated `/studio`, `/ai`, `/practice`, and `/docs`, while `/catalog?language=Spanish` and `/api/catalog` returned 200.
- Continued auth redirect hardening on 2026-05-19: unknown or missing role values now resolve to `/auth/login` instead of silently falling through to the student dashboard, reducing wrong-portal redirects when the readable role cookie is absent or malformed.
- Verification for this auth redirect pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 142 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `90d88252-a4f6-4ae7-8156-62c0c0842605`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated `/studio` and `/admin/dashboard` returned 307 redirects to `/auth/login` with preserved `next` parameters.
- Continued middleware role hardening on 2026-05-19: middleware now accepts only explicit `admin`, `teacher`, or `student` role-cookie values and redirects protected routes with missing/malformed roles to login with the original `next` path instead of falling through to the student dashboard.
- Verification for this middleware pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 145 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d3f220e2-8fe9-4469-9730-2749585e3651`. Live smoke checks returned 307 to `/auth/login?next=%2Fadmin%2Fdashboard` for a malformed role-cookie admin request, 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth role normalization on 2026-05-19: extracted shared redirect-role normalization for middleware and login, then removed the login-side fallback that could treat missing account role metadata as a student route. Login now stops with a clear account-role error when the authenticated user metadata does not include `admin`, `teacher`, or `student`.
- Verification for this auth role normalization pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 146 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `806c380a-e1a5-4022-8ba6-aad0c89993ed`. Live smoke checks returned 307 to `/auth/login?next=%2Fadmin%2Fdashboard` for a malformed role-cookie admin request, 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth role validation hardening on 2026-05-19: moved role parsing into shared auth-role helpers, reused them in redirects, middleware, and login UI, required explicit `teacher` or `student` signup roles, and blocked login session creation when a stored profile role is not a supported EdSync role.
- Verification for this auth role validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 148 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c3f9a733-47db-4455-9c01-db9a561acbfd`. Live smoke checks returned 307 to `/auth/login?next=%2Fadmin%2Fdashboard` for a malformed role-cookie admin request, 400 for an invalid `admin` self-signup role, and 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth workspace-choice hardening on 2026-05-19: added shared account-type and organization-mode normalization, then required login/signup API callers to explicitly choose `individual` or `organization` instead of silently defaulting malformed or missing values to individual; organization signup now also requires an explicit `join` or `create` mode.
- Verification for this workspace-choice pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 150 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0631232c-f560-4e7b-a178-e63ea1a997d2`. Live smoke checks returned 400 for login/signup requests missing account type, and 200 for `/auth/signup`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth client validation on 2026-05-19: the shared EdSync browser client now validates auth role, account type, and organization mode before calling the login/signup APIs, so malformed workspace choices fail locally with the same messages used by the server instead of wasting a network request.
- Verification for this client validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 152 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `628b5add-a859-47e8-91ea-a0c554000f6e`. Live smoke checks returned 200 for `/auth/login`, `/auth/signup`, `/catalog?language=Spanish`, and `/api/catalog`; login API requests missing account type still return 400 as expected.
- Continued auth HTTP contract hardening on 2026-05-19: login and signup validation failures now consistently return matching HTTP status codes instead of only embedding the status in the JSON body, and route-level tests cover incomplete login credentials and invalid signup account details.
- Verification for this auth HTTP status pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 154 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c51efaf6-e13d-4235-a445-8350d4e396da`. Live smoke checks returned real HTTP 400 responses for incomplete login credentials and short signup password, and 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued login normalization hardening on 2026-05-19: login now trims and normalizes the email before validation, so whitespace-only emails return the standard missing-credentials error before rate limits, security logging, or account lookup work runs.
- Verification for this login normalization pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 155 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `e7341c1a-a26a-43d4-84b4-265131be199e`. Live smoke checks returned HTTP 400 for whitespace-only login email, and 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued organization signup validation on 2026-05-20: organization self-signup now reuses the tenant-name length rule before tenant records are created, and generated organization slugs are capped to the EdSync lookup-code limit while preserving the unique suffix.
- Verification for this organization signup pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 157 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `ba03dae4-37da-424b-bfa0-42d68a804f20`. Live smoke checks returned HTTP 400 for an overlong organization signup name, and 200 for `/auth/signup`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth email validation on 2026-05-20: extracted shared email-address validation, reused it from the engagement email module, and added login/signup API checks so malformed, overlong, or line-break-bearing emails are rejected before rate limits, security logging, or D1 lookups.
- Verification for this auth email validation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 161 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d0bac8f5-be4d-4afa-85ac-b44ec5b75e1e`. Live smoke checks returned HTTP 400 for malformed login and signup emails, and 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth password boundary hardening on 2026-05-20: added shared signup/login password validators so signup enforces the minimum length and both auth routes reject oversized password payloads before hashing or password verification work.
- Verification for this password boundary pass: focused auth validation tests passed with 12 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 167 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `498f534e-94c7-47c0-bfb4-e46c92f51ffd`. Live smoke checks returned HTTP 400 for oversized login and signup passwords, and 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued signup display-name hardening on 2026-05-20: added shared display-name validation so optional full names are trimmed, capped to 120 characters, and rejected when they contain line breaks before profile rows or session metadata are created.
- Verification for this signup display-name pass: focused auth validation tests passed with 14 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 173 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `dcfbc187-9ca8-4153-95a6-a45a4c958fea`. Live smoke checks returned HTTP 400 for oversized and multi-line signup full names, and 200 for `/auth/signup`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued organization-code input hardening on 2026-05-20: added an early raw-input length cap before organization code normalization so public organization lookup, organization login, and organization-join signup reject oversized raw codes before slug normalization or D1 work.
- Verification for this organization-code input pass: focused organization/auth validation tests passed with 13 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 173 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0edad10a-710a-474f-90b2-2a9db84d3f4a`. Live smoke checks returned HTTP 400 for an oversized raw organization code, and 200 for `/api/auth/organizations?code=edsync`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued browser signup validation on 2026-05-20: reused the shared display-name validator in the EdSync browser client so invalid full names are rejected locally before the signup API is called.
- Verification for this browser signup validation pass: focused client/display-name tests passed with 8 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 175 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `a1a971c2-2e2a-44ac-b27f-430f4e806a17`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued browser auth credential validation on 2026-05-20: reused the shared email and password validators in the EdSync browser client so malformed emails, short signup passwords, and oversized login/signup passwords are rejected locally before auth API calls.
- Verification for this browser auth credential pass: focused client/email/password tests passed with 14 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 179 tests. The Cloudflare deploy command timed out locally after upload, but `wrangler deployments list` confirmed the existing Worker `edsync-dev` deployed Version ID `dbb50696-773b-4a86-a894-c07e43272684`; live URL is `https://edsync-dev.learn-app.workers.dev`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued browser organization-code validation on 2026-05-20: reused the shared organization-code validator in the EdSync browser client so organization login and organization-join signup reject missing or oversized organization codes before auth API calls.
- Verification for this browser organization-code pass: focused client/organization-code tests passed with 14 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 182 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `164f3f05-dde0-4ea5-b18e-5fc6fcba9030`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued auth page organization lookup validation on 2026-05-20: login and signup organization lookup effects now reuse the shared organization-code validator before calling `/api/auth/organizations`, and the signup step guard surfaces the same validation message before users continue.
- Verification for this auth page lookup pass: focused client/organization-code tests passed with 14 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 182 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `b76f618b-8e07-4252-854f-a5384d14bfc6`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued browser organization-name validation on 2026-05-20: organization-create signup now reuses tenant-name validation in the EdSync browser client and signup step guard, mapping tenant wording to organization wording before API calls.
- Verification for this browser organization-name pass: focused client/tenant validation tests passed with 17 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 184 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `37257abb-6c31-4e92-a124-5c034ac4f3db`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued profile display-name validation on 2026-05-20: teacher and student profile save flows now reuse the shared display-name validator so saved profile names are trimmed, capped to 120 characters, and blocked when multi-line before profile updates.
- Verification for this profile display-name pass: focused display-name tests passed with 4 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 184 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `00a87208-059a-4653-914c-7558407bd775`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`.
- Continued profile detail validation on 2026-05-21: added shared profile-field validators for school, grade level, subject areas, and interest areas; teacher and student profile save flows now normalize supported values, reject multi-line or oversized profile text, deduplicate list selections, and block unsupported list values before profile updates.
- Verification for this profile detail pass: focused profile/display-name tests passed with 9 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 189 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `38cf93e3-0750-4684-b74a-650a3c0181cf`. Live smoke checks returned 200 for `/auth/signup`, `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/teacher/profile` and `/student/profile` returned 307 redirects for unauthenticated users as expected.
- Continued lesson design and AI workflow organization on 2026-05-21: extracted the Studio lesson design catalog into `src/lib/learning/design-system.ts`, expanded reusable themes, template presets, slide layouts, transitions, animations, practice game/rubric templates, and media safety blocks, then wired AI course workflow generation to request design specs, reduced-motion fallbacks, practice plans, review cards, points, explanations, and teacher-review metadata. The Studio catalog now re-exports the shared design system instead of owning the large design block directly.
- Verification for this lesson design workflow pass: focused design-system tests passed with 4 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 193 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0720cbc6-dc7a-4057-97ad-e1fca7c08f41`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai`, `/studio`, and `/teacher/lessons/create` returned 307 redirects for unauthenticated users as expected.
- Continued ops/build script cleanup on 2026-05-21: added `scripts/lib/ops.mjs` for shared env loading, Windows-safe command resolution, and child-process execution, then refactored the Vercel deployment script to use the helper instead of maintaining duplicate env/process plumbing. Generated build folders and `node_modules` remain ignored/disposable; no Cloudflare D1/R2/worker data was deleted because no exact safe deletion target was specified.
- Verification for this ops cleanup pass: helper import check passed, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 193 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `dc1d0b43-d26f-4e3d-8dcf-4d8ca574152b`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected.
- Continued Cloudflare deploy script cleanup on 2026-05-21: refactored `scripts/deploy-cloudflare.mjs` to reuse the shared ops helper for environment loading, Windows-safe command resolution, stdin handling, and child-process execution, removing duplicate deploy plumbing while preserving existing Pages, app Worker, automation Worker, and secret behavior.
- Verification for this Cloudflare script pass: `node --check scripts\deploy-cloudflare.mjs`, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 193 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `ef85215f-3191-44d3-9cc0-66e4e4a1ec45`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected.
- Continued D1 migration script cleanup on 2026-05-21: refactored `scripts/d1-migrate.mjs` to reuse shared ops helpers for `.env.local`/`.env` loading and Windows-safe Wrangler execution, preserving migration order, seed behavior, and the existing `CLOUDFLARE_D1_DATABASE_NAME` fallback. Remote migrations were not executed during this cleanup pass to avoid mutating D1 data without a specific migration request.
- Verification for this D1 script pass: `node --check scripts\d1-migrate.mjs`, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 193 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `138e3f63-9a8a-4667-808d-328f816fde8b`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected.
- Continued lesson template and AI insert-back refinement on 2026-05-21: expanded reusable lesson template metadata with best-fit contexts, design notes, practice modes, review signals, reusable block summaries, and compact template options for UI/AI controls. AI Studio insert-back now formats generated lesson workflow output into escaped editable sections for outline, design, modules, practice plan, quiz, rubric, tags, and teacher review, plus structured slide outlines and sheet metadata instead of a single raw JSON block.
- Verification for this template/AI insert-back pass: focused design-system tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 194 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9076747c-3e67-4dda-bf20-ef1fb15df587`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected.
- Continued Cloudflare D1 script helper cleanup on 2026-05-21: added `scripts/lib/cloudflare-d1.mjs` for shared `.env.local`/`.env` loading and D1 REST query execution, then refactored admin creation and AI provider seeding scripts to use it instead of duplicating Cloudflare D1 env/query plumbing. Admin creation and provider seeding were not executed during this cleanup pass to avoid mutating D1 data without a specific data operation request.
- Verification for this D1 helper pass: `node --check scripts\lib\cloudflare-d1.mjs`, `node --check scripts\create-admin.mjs`, `node --check scripts\seed-ai-providers.mjs`, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 194 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `12c5b8b1-ad23-4308-99cc-e9a5ec2ca664`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected.
- Continued ops and lesson design cleanup on 2026-05-21: added `scripts/lib/cloudflare-api.mjs` so Cloudflare API scripts share token loading, optional GETs, JSON error handling, and request execution. Refactored `scripts/cloudflare-security.mjs` to use the shared helper and remove duplicated fetch/env code. Expanded the lesson design system with output length controls, compact authoring tool groups, Creator Studio and Exam Prep presets, animation/AI response templates, and AI practice-loop blocks so templates can drive lesson design, transitions, animations, quizzes, practice, and review context more concretely.
- Verification for this ops/template pass: `node --check scripts\lib\cloudflare-api.mjs`, `node --check scripts\cloudflare-security.mjs`, focused design-system tests passed with 7 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 196 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `2b6eba8c-aaf0-45c8-ac07-ae62a667af41`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed in this pass because no exact deletion target was specified.
- Continued AI workflow and template integration on 2026-05-21: wired the new lesson output length controls through the Studio AI prompt catalog, AI prompt builder request payload, `/api/ai/course-workflow`, and the course workflow prompt generator. The AI workflow now passes micro/standard/extended expectations into outline, module, and practice-plan steps alongside template, tool-group, reusable-block, animation, transition, quiz, and review context. Added a focused unit test to guard that the generated prompts include the selected output length, tool groups, reusable blocks, practice counts, and practice mode.
- Verification for this AI workflow integration pass: focused AI workflow/design tests passed with 8 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 197 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `e0818a5f-9ca5-4fcd-8a2d-262ba9e142ce`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed in this pass because no exact deletion target was specified.
- Continued Studio design library integration on 2026-05-21: moved design/template insertion into reusable Studio workspace action helpers, removed the hard five-template limit in the Studio sidebar, and made design templates and section blocks insert richer editable content with IDs, categories, tones, block lists, targets, estimated minutes, and structured block metadata. This makes animation packs, AI lesson responses, practice loops, and newer templates usable from Studio instead of hidden behind the earlier slice limit.
- Verification for this Studio design integration pass: focused Studio/design tests passed with 13 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 198 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `adae0725-f37d-4838-98c8-71f052726eac`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed in this pass because no exact deletion target was specified.
- Continued teacher lesson generation/template integration on 2026-05-21: added a shared lesson generation design context helper that maps teacher depth choices to micro/standard/extended output lengths and injects EdSync template, theme, transition, animation, practice, review, and reduced-motion instructions into `/api/ai/create-lesson`. The teacher lesson creator now has a Lesson Design selector backed by the shared lesson template options, persists it in local drafts, and sends both `designTemplateId` and `outputLength` to lesson generation so classic lesson creation and Studio AI use the same design vocabulary.
- Verification for this teacher lesson generation pass: focused lesson-design/design-system tests passed with 9 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 200 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0862256c-eac1-4396-b23e-7005fc3f3dba`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed in this pass because no exact deletion target was specified.
- Continued Practice loop refinement on 2026-05-21: expanded `PRACTICE_MODES` with mode-specific loop steps, best-use guidance, and expected output metadata for quiz, exam, flashcards, matching, sprint, mistake retry, fill-in-the-blank, true/false, and generated-from-Studio practice. Updated the Practice workspace so mode cards show target time and loop flow, the Timing panel explains the selected mode's use/output, and the Learning Loop panel changes with the active mode instead of showing one generic sequence.
- Verification for this Practice loop pass: focused practice mode/engine tests passed with 7 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 201 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `232e61cd-5452-408c-849d-c802b0708697`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed in this pass because no exact deletion target was specified.
- Continued Practice attempt context refinement on 2026-05-21: added a Practice-domain attempt context helper so saved attempts, attempt items, review cards, and learning-event payloads retain mode label, target timing, loop steps, best-use guidance, output expectations, source context, review-card count, and a dashboard-ready recommendation without changing the existing score summary shape.
- Verification for this Practice attempt context pass: focused `attempt-context` and `modes` tests passed with 7 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 204 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `585d874d-1fbe-4597-9c49-e89e4519c309`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued Practice review/recommendation refinement on 2026-05-21: added a review-card mapper that safely parses stored metadata into stable mode, loop, source, and next-action fields; the review API now returns normalized cards, the browser client shares that type, and the Practice review queue shows mode chips plus loop guidance when available while legacy cards stay uncluttered.
- Verification for this Practice review pass: focused `review-cards` and `attempt-context` tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 206 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `54e8481b-7e02-46ca-8828-5acb45bddd9b`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai` and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued student dashboard recommendation refinement on 2026-05-21: added a reusable Practice review recommendation helper that turns pending review cards into one clear next action, connected the student dashboard to fetch review cards with the existing planner/profile load, replaced the static score metric with a real review count, and surfaced a compact Review Now card above the lesson recommendation when missed practice needs attention.
- Verification for this student dashboard review pass: focused `review-recommendations` and `review-cards` tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 209 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `208deecb-2768-4fe6-8edf-313318203a87`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai`, `/studio`, and `/student/dashboard` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued teacher dashboard review-signal refinement on 2026-05-21: added a teacher Practice review signal helper that summarizes pending student review cards by count, mastery state, and top practice mode. The teacher dashboard now queries review cards only for students in the teacher's active classes, normalizes the stored card metadata, replaces the static AI-chat metric with a real review count, and points the Today panel at the scoped review queue.
- Verification for this teacher dashboard review pass: focused `teacher-review-signals` and `review-recommendations` tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 211 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3fee888b-59ba-43fc-bc4b-eca9c5916bbd`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/ai`, `/studio`, and `/teacher/dashboard` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued teacher analytics review-signal refinement on 2026-05-21: connected the teacher Analytics page to the same scoped Practice review signal used by the dashboard, added pending review cards to the top metric row, surfaced a Practice review queue card inside Interventions, and cleaned visible mojibake ellipses in that page so loading/truncated labels render cleanly.
- Verification for this teacher analytics review pass: focused `teacher-review-signals` and `review-recommendations` tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 211 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3ba48542-cce8-4f27-82a3-f48cb0aff697`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/teacher/analytics`, `/teacher/dashboard`, and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued AI analytics intervention refinement on 2026-05-21: the teacher Analytics page now sends its scoped Practice review signal with AI intervention requests, and `/api/ai/analytics` normalizes that review evidence into bounded pending/again/almost counts, top review mode, and review status text before inserting it into the instructional-coach prompt.
- Verification for this AI analytics review pass: focused `teacher-review-signals` and `review-recommendations` tests passed with 5 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 211 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `6de2dbee-de0d-4b5e-a451-0bb73b6bcd31`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; protected `/teacher/analytics`, `/teacher/dashboard`, and `/studio` returned 307 redirects for unauthenticated users as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued teacher review data-boundary refinement on 2026-05-21: added `/api/teacher/review-signal` so teacher dashboard and analytics no longer read `practice_review_cards` directly through generic browser table access. The server route resolves the tenant, requires teacher/admin access, scopes review cards to active enrolled students in active teacher-owned classes, and returns the same compact Practice review signal used by dashboard, analytics, and AI intervention prompts.
- Verification for this teacher review data-boundary pass: focused `teacher-review-signals` tests passed with 4 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 213 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `31d76f8c-c792-469b-99af-301078d9a3ec`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; `/api/teacher/review-signal` returned 403 for unauthenticated access; protected `/teacher/analytics`, `/teacher/dashboard`, and `/studio` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued teacher roster tenant-boundary refinement on 2026-05-21: updated `/api/teacher/roster` to resolve the active tenant before returning classes/students, use `tenant_object_links` for tenant-scoped class ownership, preserve legacy default-tenant unlinked classes, and keep teacher users limited to their active classes while platform admins can see the active tenant roster.
- Verification for this teacher roster pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 213 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `8e5b8e90-64d4-47bb-8ec3-7d9693f7c07f`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; `/api/teacher/roster` returned 403 for unauthenticated access; protected `/teacher/gradebook`, `/teacher/work`, and `/studio` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued tenant object-boundary cleanup on 2026-05-21: added a reusable tenant object SQL scope helper for `tenant_object_links`, refactored `/api/teacher/roster` onto it, and updated `/api/notifications/lesson-assigned` so lesson lookup, class lookup, class recipients, and direct student recipients are all constrained to the active tenant while preserving legacy default-tenant unlinked objects.
- Verification for this tenant object-boundary pass: focused `object-scope` tests passed with 3 tests, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5fdb4e9a-866f-46a8-a7e2-8c56f813ad7c`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated `/api/notifications/lesson-assigned` returned 401; unauthenticated `/api/teacher/roster` returned 403; protected `/teacher/lessons` and `/studio` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued email recipient boundary refinement on 2026-05-21: updated `/api/email/send` so class-recipient expansion uses the shared tenant object SQL scope, keeping direct email recipients unchanged while preventing class email sends from crossing the active tenant boundary.
- Verification for this email recipient pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `cddae58b-6198-481e-b520-be16ab6db2e0`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated `/api/email/send` returned 401; protected `/admin/email`, `/teacher/students`, and `/studio` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued planner tenant-boundary refinement on 2026-05-21: updated `/api/planner` so teacher class lookup, class student notification targets, teacher announcement/event reads, and student class calendar reads all resolve active tenant context through the shared tenant object scope while preserving owner-scoped personal student study events.
- Verification for this planner boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `b9bbf74c-ad4a-48c1-8c91-a9490dee5608`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/planner` returned 401; protected `/teacher/planner` and `/student/dashboard` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued work/submission tenant-boundary refinement on 2026-05-21: updated `/api/work` and `/api/work/submissions` so work listings, teacher submission views, student submission history, student submission creation, and teacher grading all resolve active tenant context through the shared tenant object scope. Student submission creation now also requires active enrollment when the work item belongs to a class, while tenant-wide non-class published work remains available.
- Verification for this work/submission boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `449290a3-9fb5-4666-8ffb-2372baf97fee`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated `/api/work` and `/api/work/submissions` returned 401; protected `/teacher/work` and `/student/work` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued discussion tenant-boundary refinement on 2026-05-21: updated `/api/discussions` so thread lists, post reads, thread creation, and post creation resolve active tenant context through `tenant_object_links` for discussion threads/classes. New standalone threads are linked to the active tenant, class threads require class ownership for teachers, students only see class threads through active enrollment or tenant-visible classless threads, and locked threads reject non-manager posts.
- Verification for this discussion boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c10ad7f1-2461-4b40-9de5-780bff74cb87`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/discussions` returned 401; protected `/teacher/discussions` and `/student/discussions` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued student notes tenant-boundary refinement on 2026-05-21: updated `/api/notes` so teacher/admin note reads, student-visible note reads, and new note creation resolve active tenant context through `tenant_object_links` for student notes/classes. Teachers can only create notes for students enrolled in their active tenant-scoped classes, platform admins can create notes for active tenant student members, and new note records are linked to the active tenant after insert.
- Verification for this student notes boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `0de4ae21-fb48-443d-b64e-51ba4edf54e9`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/notes` returned 401; protected `/teacher/notes` and `/student/notes` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued gradebook tenant-boundary refinement on 2026-05-21: updated grade event materialization to link `gradebook_scores` into the active tenant, scoped `/api/grades` category and score reads through tenant object links/classes, required tenant-owned class access before category creation, verified manual grade student/class/category relationships before recording grade events, and scoped lesson quiz grade recording to tenant-visible lessons plus active class enrollment.
- Verification for this gradebook boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9bf4e5fb-913a-4ece-89ea-1408dccaf9ae`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/grades` and POST `/api/grades/lesson-quiz` returned 401; protected `/teacher/gradebook` and `/student/grades` returned 307 redirects as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued Practice review boundary refinement on 2026-05-21: updated `/api/teacher/review-signal` so the class enrollment check also scopes classes through active tenant object links, then updated `/api/practice/attempts` to validate known database-backed sources before saving attempts. Studio sources must belong to the active tenant and current owner or be published, lesson sources must be tenant-visible, and class lessons require active enrollment. New practice attempts are also linked into `tenant_object_links` for future tenant-boundary queries.
- Verification for this Practice review boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5e72be44-76a5-4c0a-882f-41b5c6012b2d`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/practice/attempts` and GET `/api/reviews` returned 401; unauthenticated `/api/teacher/review-signal` returned 403; protected `/practice` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued offline sync tenant-boundary refinement on 2026-05-21: updated offline sync idempotency so duplicate client sync checks are scoped by tenant, user, and client id instead of only user/client id. This prevents a browser-generated offline client id from replaying or masking work across organizations when the same user switches active tenant context.
- Verification for this offline sync boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `6c9f1086-f4a4-41f6-aac7-8533efeac21d`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/offline-sync`, GET `/api/events`, and POST `/api/events` returned 401; protected `/studio` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued learning-event source boundary refinement on 2026-05-21: updated `/api/events` so known EdSync source ids are validated before learning events are appended. Studio and content-block sources must belong to the active tenant and current owner, be published, or be viewed by platform admin; lesson and work-item sources must be tenant-visible and either teacher-owned, admin-visible, classless, or reachable through active enrollment; practice sources must belong to the current student in the active tenant. Unknown source families remain accepted for generic audit events.
- Verification for this learning-event source pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `05c41eee-2888-4980-ae93-1e2a74cae45b`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/events` and POST `/api/offline-sync` returned 401; protected `/studio` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued content/media tenant-boundary refinement on 2026-05-21: updated uploads so R2 keys include the active tenant path segment and both `storage_objects` and `media_assets` are linked through `tenant_object_links`. Updated `/api/content-blocks` so non-admin users only list their own blocks plus published tenant blocks, and hard-deleting a block now removes its tenant object link before deleting the block row.
- Verification for this content/media boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `51094442-6c17-40bd-8955-a615b516bee5`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated GET/POST `/api/content-blocks` and POST `/api/storage/upload` returned 401; protected `/studio` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued content ingestion and standards boundary refinement on 2026-05-21: updated content extraction saves so new `content_extractions` rows carry active tenant metadata and are linked through `tenant_object_links` without changing the schema. Updated `/api/standards` so packages can only reference an uploaded `storage_objects` row when that storage object is tenant-linked and owned by the current user, and centralized the standards package table name used for link cleanup/creation.
- Verification for this ingestion/standards boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `b75aa7ec-5724-4e85-96aa-bfe113eca726`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/content/extract`, GET `/api/standards`, and POST `/api/standards` returned 401; protected `/admin/standards` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued permissions tenant-boundary refinement on 2026-05-21: scoped role profile permission resolution so tenant memberships can only load profiles from the active tenant, global null-tenant profiles, or built-in system profiles. Updated `/api/permissions` so built-in system profiles remain visible across organizations and tenant role-profile creation returns a clear duplicate-key conflict before insertion.
- Verification for this permissions boundary pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 216 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `f08ca0df-ebf1-4a5a-a31c-9c8a9c774b9f`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated `/api/permissions` returned 401; protected `/admin/permissions` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued generic data-access hardening on 2026-05-21: made `/api/data` authorization asynchronous and added teacher-owned write verification for legacy generic writes against lessons, classes, lesson sections, quiz questions, glossary terms, and lesson assignments. Lesson authoring writes now verify teacher ownership through D1 before update/delete/insert, reducing the blast radius of remaining browser table callers while feature APIs continue to replace them.
- Verification for this generic data-access pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 220 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `540624c7-6fa6-4824-96b4-9434791920e4`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/data` returned 401; protected `/teacher/lessons` and `/studio` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued student data-access hardening on 2026-05-21: expanded the legacy `/api/data` guard so student-owned learning records cannot be updated or deleted unless the request is explicitly scoped to the signed-in student or the target row is verified by id. This now covers student progress, learning goals, reflections, quiz attempts, Socratic interactions, and knowledge nodes while preserving existing student lesson/player flows.
- Verification for this student data-access pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 223 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `3a1d984f-0755-4d6f-a0e4-e6631729622c`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/data` returned 401; protected `/student/dashboard` and `/student/lessons/fake` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued class enrollment and assignment data-access hardening on 2026-05-21: added legacy `/api/data` ownership checks for `class_enrollments`, allowing students to join only as themselves and teachers to manage enrollments only for their own classes. Also added assignment-by-id ownership verification so existing teacher assignment removal flows continue to work while still requiring teacher-owned lesson/assignment scope.
- Verification for this enrollment/assignment guard pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` all passed with 226 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `73ef500f-665c-47ee-bf55-c1f0211a6568`. Live smoke checks returned 200 for `/auth/login`, `/catalog?language=Spanish`, and `/api/catalog`; unauthenticated POST `/api/data` returned 401; protected `/teacher/students` and `/student/dashboard` returned 307 as expected. No Cloudflare D1/R2 data cleanup or deletion was performed because no exact safe deletion target was specified.
- Continued local workspace cleanup on 2026-05-21: added a repeatable cleanup script for generated local artifacts, wired package scripts for safe cleanup and dependency cleanup, documented the cleanup workflow, and removed rebuildable `.next`, `.open-next`, `.wrangler`, and TypeScript build-info output from the local workspace. `node_modules` and `.vercel` were intentionally kept so local commands and project linking remain usable.
- Verification for this local cleanup pass: `node --check scripts\clean-local.mjs`, `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run test` all passed with 226 tests. `npm.cmd run build` was intentionally not run after cleanup because it would recreate the large `.next` output that this pass removed. No Cloudflare D1/R2 data cleanup or deletion was performed.
- Continued notes workspace refinement on 2026-05-22: upgraded the teacher Notes page into two lanes: personal teaching notes saved as Studio drafts, and student feedback notes that remain connected to the existing student-note API. Teaching notes now support safe HTTPS media/link validation, design presets, edit, duplicate, archive, Studio handoff, and dark/light-safe premium surfaces.
- Verification for this teacher notes pass: `npm.cmd run typecheck`, focused Studio/media tests, `npm.cmd run lint`, and `npm.cmd run build` all passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `79f62702-a892-4875-8d81-b0f15e47b9e4`. Live smoke checks returned 200 for `/api/catalog`, 401 for unauthenticated `/api/notes`, and protected `/teacher/notes` plus `/student/notes` returned 307 redirects as expected. Generated local build/deploy folders were cleaned after verification.
- Continued Analytics & Reports cleanup on 2026-05-22: polished the teacher lesson reports page with premium shell surfaces, real lucide metric/action icons, a clearer lesson selector card, a better empty state, responsive summary cards, record count context, and ASCII-safe missing-value placeholders. This keeps `/teacher/reports` as a stable detailed export route while the sidebar continues to present Analytics & Reports as one combined area.
- Verification for this reports UI pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and focused teacher analytics/review tests passed. Deployment was deferred until the next broader pass because this was a small UI-only route update.
- Continued admin portal responsiveness cleanup on 2026-05-22: widened the Portals page container, upgraded portal creation and directory cards to premium surfaces, added a compact organization context block, improved wrapping for portal/domain text, moved portal metadata into a contained status card, and replaced hardcoded light-only badges with theme-safe EdSync tokens.
- Verification for this admin portals pass: `npm.cmd run typecheck` and `npm.cmd run lint` passed. Deployment was deferred until the next broader pass because this was a small UI-only route update.
- Continued portal workflow planning and lesson authoring cleanup on 2026-05-23: added Phase 24 to `plan.md` for personal multimedia notes, student notification/widget toggles, merged Practice + AI support, class-scoped work/planner/discussion flows, individual marketplace behavior, admin view-as sidebar continuity, and Canva/Kahoot-inspired lesson/activity editing. Also converted shared lesson section templates and teacher lesson-create insert tools from raw HTML snippets into readable block text, and normalized legacy HTML draft content before it appears in the manual section editor.
- Verification for this authoring cleanup pass: `npm.cmd run typecheck`, `npm.cmd run lint`, focused Studio/media tests, and `npm.cmd run build` passed. Deployment is queued for the next broader portal/UI pass so the remaining sidebar and merged Practice + AI work can ship together.
- Continued Practice + AI navigation consolidation on 2026-05-23: removed Studio and standalone AI from primary teacher/student sidebar navigation, renamed the shared support entry to Practice + AI, added an embedded AI Tutor prompt-builder panel inside the Practice workspace, allowed `/practice?ai=1&task=...` to open that panel directly, and changed `/ai` into a compatibility redirect into the merged support surface. Existing `/quizzes` and `/games` aliases continue to route into Practice modes.
- Verification for this merged support pass: `npm.cmd run typecheck`, `npm.cmd run lint`, focused practice tests, and `npm.cmd run build` passed. Deployment is still queued for the next broader portal/sidebar pass.
- Continued student dashboard personalization on 2026-05-23: expanded dashboard visibility preferences with Practice + AI, Grades, and Feedback toggles; renamed class-update wording to Notifications; hid practice recommendations when Practice + AI is turned off; and added compact support shortcuts that respect the student's selected dashboard sections.
- Verification for this student dashboard pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` passed. Deployment remains queued for the next grouped portal release.
- Deployed the grouped Phase 24 portal/navigation update to the existing Cloudflare Worker on 2026-05-23. Live URL: `https://edsync-dev.learn-app.workers.dev`; Version ID: `43705cb6-95af-410e-937c-6dfb6e4c3a02`. Live smoke checks returned 200 for `/api/catalog` and 307 redirects for unauthenticated `/ai`, `/practice`, and `/student/dashboard`. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued master UX and lesson-studio refinement on 2026-05-23: added the consolidated Canva/Kahoot/AI portal brief into `plan.md`, upgraded the teacher lesson creator into a Canva-style Lesson Creation Studio shell with a vertical tool rail, contextual inspector panels, template/block/media/text/brand/animation/export controls, and a bottom page strip, then aligned teacher/student sidebar labels around My Courses, Create Lesson, Assignments, Gradebook & Feedback, and Practice + AI Tutor.
- Verification for this master UX/studio shell pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` passed. Deployment is still pending for this latest slice; the last live Cloudflare Worker remains `https://edsync-dev.learn-app.workers.dev` at Version ID `43705cb6-95af-410e-937c-6dfb6e4c3a02`.
- Continued lesson authoring editor cleanup on 2026-05-23: added a reusable visual `LessonBlockEditor` for manual lesson sections and connected the teacher lesson creator to it. Text/activity/discussion/quiz sections now use a Canva-style block canvas with insert chips, paste-as-plain-text behavior, legacy HTML normalization, and a live structure panel instead of exposing raw HTML-like content in the main editing path.
- Verification for this lesson block editor pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `de01cd5b-23c0-4507-af91-e0d26c8c9edc`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/teacher/lessons/create`, `/practice`, and `/ai` returned 307 redirects for unauthenticated users as expected. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued student portal notification refinement on 2026-05-23: moved dashboard notification visibility into a shared student preference helper, added a circular active-time gauge to replace plain metric treatment, improved paused-notification behavior, added a dedicated `/student/notifications` settings page with the full sidebar, and added Notifications to the student Account navigation group.
- Verification for this student notification pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `83c23661-6f92-48a7-9b09-554c323226eb`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/student/notifications`, `/student/dashboard`, and `/practice` returned 307 redirects for unauthenticated users as expected. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued gradebook feedback and release-control refinement on 2026-05-23: updated `/api/grades` so student grade reads exclude draft scores and weighted averages count only released graded scores. The teacher Gradebook now supports rich feedback snippets, a feedback textarea, and a visible-to-student release checkbox so teachers can save private draft grades before publishing them to students.
- Verification for this gradebook release pass: `npm.cmd run typecheck`, `npm.cmd run lint`, and `npm.cmd run build` passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `5361ad43-98af-4362-bc2f-68e431e96577`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/teacher/gradebook` and `/student/grades` returned 307 redirects for unauthenticated users; unauthenticated `/api/grades` returned 401. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued Canva-style creator library refinement on 2026-05-23: added a shared creator library for template panels, element categories, text styles, palette swatches, export targets, note design presets, and Kahoot-style practice game presets. The teacher Lesson Creation Studio now uses that shared library for media, text, brand, export, and practice-game block panels instead of keeping each list hardcoded. Student and teacher Notes now share the same design preset source, and teacher personal notes gained the same secure media upload path already used by student notes.
- Verification for this creator library pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `d1f046b0-9ec9-48c4-9fa8-46fcd1a4f24c`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/teacher/lessons/create`, `/teacher/notes`, and `/student/notes` returned 307 redirects for unauthenticated users; GET `/api/storage/upload` returned 405 as expected because uploads require POST. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued lesson block editor and notes view-mode refinement on 2026-05-23: expanded the manual `LessonBlockEditor` with creator-library driven text-style inserts, practice card inserts, palette markers, and media cues so lesson sections are composed as visual learning blocks instead of raw HTML-style text. Added shared grid/list view-mode preferences and applied them to student and teacher personal notes so notes can behave more like a multimedia workspace.
- Verification for this block editor and notes view pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 226 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `c0bd9859-594f-46a7-8cf2-cae2d8f22b3f`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/teacher/lessons/create`, `/teacher/notes`, and `/student/notes` returned 307 redirects for unauthenticated users. Generated `.next`, `.open-next`, `.wrangler`, and `tsconfig.tsbuildinfo` artifacts were removed with `npm.cmd run clean:local` after deployment.
- Continued Work and grade semantics refinement on 2026-05-24: added shared work grading settings for points, weighted course-grade contribution, completion-only evidence, and participation evidence. The Work API now persists those settings in `learning_work_items.settings`, includes them in planner/notification/event metadata, and prevents completion/participation feedback from creating gradebook score events while still recording learning-event audit evidence. Teacher Work now exposes scoring controls and student Work shows clear scoring labels.
- Verification for this Work scoring pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 228 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `43ca19d6-770a-4efc-9618-5029b133cd4f`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/teacher/work`, `/student/work`, `/teacher/lessons/create`, and `/practice` returned 307 redirects for unauthenticated users as expected. The first deploy attempt hit the local two-minute timeout with no output; rerunning the same deploy with a longer timeout succeeded. Generated local build/deploy artifacts will be cleaned after this progress entry is committed.
- Continued previous-session verification cleanup on 2026-05-24: audited the sidebar/shared-route requirements, removed the standalone `/studio` destination by turning it into a role-aware compatibility redirect, moved teacher Practice + AI Tutor into its own Support group, changed visible Practice copy away from the old practice-workspace label, pointed AI saved drafts to `/notes`, removed remaining public preview authoring tags in favor of lessons, updated docs/sheets/slides metadata, and changed AI lesson-generation instructions from raw HTML/workspace wording to EdSync learning-block wording.
- Verification for this route/copy/navigation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 228 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `4fc0294e-7231-4c68-8e35-441bcf75649b`. Live smoke checks returned 200 for `/api/catalog`, `/catalog`, `/showcase`, and `/auth/login`; unauthenticated `/studio`, `/ai`, `/practice`, and `/teacher/lessons/create` returned 307 redirects as expected. Remaining known risk: internal component/type/API names still use the historical Studio data model because they back notes/docs/slides drafts; a deeper file/folder rename should be done as a separate compatibility migration.
- Continued legacy-route and admin-view context verification on 2026-05-24: removed the visible authoring tab from the shared draft catalog, renamed generated practice to "Generated from materials", pointed the public workflow lesson slide at `/teacher/lessons/create`, and preserved `adminView` plus AI task context through `/ai`, `/studio`, `/games`, and `/quizzes` compatibility redirects. This keeps older links stable while reducing the chance that admin view-as navigation drops the user into the wrong shell.
- Verification for this legacy-route pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 228 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `a61072b7-fa44-4fd0-b05d-99dfc4528b03`. Live signed-out smoke checks returned 200 for `/api/catalog` and `/showcase`; `/studio?adminView=teacher`, `/ai?task=generate-practice&adminView=teacher`, `/games?adminView=student`, `/quizzes?adminView=student`, and `/practice` returned 307 redirects to login with the correct `next` target preserved. Signed-in admin-view redirect branches still need authenticated browser QA in a later pass.
- Continued previous-session UX verification on 2026-05-24: removed learner-facing XP from AI personalization prompts, renamed teacher planner UI from announcements to class notifications while preserving the existing announcement table/API contract, and pointed the lesson editor's Generate practice CTA to the merged Practice + AI Tutor workspace instead of the legacy generated-from-Studio route mode. A follow-up scan found only internal compatibility identifiers for `generated_from_studio` remaining.
- Verification for this UX copy/navigation pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 228 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `9ac3b47d-c35e-4a6c-ae66-319025c8540d`. Live smoke checks returned 200 for `/api/catalog`, `/showcase`, and `/catalog`; protected `/teacher/planner`, `/teacher/lessons/create`, `/practice?mode=quiz&ai=1&task=generate-practice`, `/studio?adminView=teacher`, and `/ai?task=generate-practice&adminView=teacher` returned 307 redirects with intended `next` targets preserved. Signed-in admin-view redirect branches still need authenticated browser QA in a later pass.
- Continued XP model cleanup on 2026-05-24: stopped student lesson completion from awarding `total_xp`, removed the stale `increment_xp` RPC shim from the generic D1 data adapter, and removed `total_xp` from the admin users API response. Remaining `total_xp` references are compatibility schema/default fields in migrations, seed/admin creation, signup insert defaults, and the shared profile type, not active UI or reward logic.
- Verification for this XP cleanup pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 228 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `6074a4b8-a33c-41fb-9495-2bf943b60ce6`. Live smoke checks returned 200 for `/api/catalog` and `/auth/login`; protected `/admin/users`, `/student/lessons/fake`, `/practice?mode=quiz&ai=1&task=generate-practice`, and `/teacher/lessons/create` returned 307 redirects with intended `next` targets preserved; unauthenticated `/api/admin/users` returned 401.
- Continued Practice + AI compatibility cleanup on 2026-05-24: added canonical `generated_from_materials` practice mode, moved the visible generated-practice catalog entry to that mode, normalized legacy `generated_from_studio` query/API values into the materials mode, and taught practice context helpers to display old saved metadata as Generated from materials instead of falling back to Quiz.
- Verification for this Practice + AI compatibility pass: `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 229 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `68e3a081-1e4f-4bb9-ac8b-d5863a2595e7`. Live smoke checks returned 200 for `/api/catalog` and `/catalog`; protected `/practice?mode=generated_from_materials&ai=1&task=generate-practice`, `/practice?mode=generated_from_studio&ai=1&task=generate-practice`, `/games?adminView=student`, `/quizzes?adminView=student`, and `/teacher/lessons/create` returned 307 redirects with intended `next` targets preserved.
- Continued workspace wording verification on 2026-05-24: removed the remaining user-facing old authoring labels from shared saved-materials routes and API errors, changed the workspace AI action to the merged Practice + AI surface, updated the workflow/navigation docs, and rescanned source/docs/progress for stale visible legacy workspace phrases.
- Verification for this workspace wording pass: the stale visible-label scan returned no matches, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` passed with 229 tests. Redeployed the existing Cloudflare Worker `edsync-dev`; live URL is `https://edsync-dev.learn-app.workers.dev` and Version ID is `2739703c-e5a5-4638-a37a-e06c504f7700`. Live smoke checks returned 200 for `/api/catalog` and `/catalog`; protected `/notes`, `/docs`, `/slides`, `/practice?ai=1&task=clean-notes`, and `/studio?adminView=teacher` returned 307 redirects with intended `next` targets preserved. Deploy completed with the existing OpenNext-on-Windows, Vectorize-local, multiple-environment, generated duplicate-key, and Node child-process warnings; no new worker or resource was created.

## Update Protocol

After each implementation pass:
- Update the relevant phase status.
- Add verification results.
- Add decisions that affect architecture, prompts, templates, or data models.
- Record blockers with the next concrete action.
- Commit `progress.md` separately and push `main`.
